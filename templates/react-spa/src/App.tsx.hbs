function App() {
  return (
    <div style=\{{ fontFamily: "system-ui, sans-serif", maxWidth: 800, margin: "0 auto", padding: "2rem" }}>
      <h1>{{name}}</h1>
      <p style=\{{ color: "#666" }}>{{description}}</p>
      <p>Your React app is running! Edit <code>src/App.tsx</code> to get started.</p>
    </div>
  );
}

export default App;
