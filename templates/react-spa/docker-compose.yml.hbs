services:
  app:
    build: .
    container_name: aigo-{{orgSlug}}-{{slug}}-prod
    restart: unless-stopped
    ports:
      - "{{prodPort}}:80"
    {{#if envVars}}
    environment:
      {{#each envVars}}
      {{@key}}: "{{this}}"
      {{/each}}
    {{/if}}
