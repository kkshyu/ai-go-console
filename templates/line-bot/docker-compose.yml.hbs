services:
  app:
    build: .
    container_name: aigo-app-{{slug}}
    restart: unless-stopped
    ports:
      - "{{port}}:3000"
    environment:
      PORT: "3000"
      NODE_ENV: "production"
      {{#each envVars}}
      {{@key}}: "{{this}}"
      {{/each}}
